// Placeholder screen for the center tab button.
// The actual button is overridden in _layout.tsx via tabBarButton → router.push('/create-event').
// This file must exist so Expo Router doesn't throw a missing-route error.
export default function CreateScreen() {
  return null;
}
